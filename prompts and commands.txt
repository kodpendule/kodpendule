In attachment you can see folders and files of mine Django project with the codes inside them. I did not exported content from all files, just the onces that are relevant for what I need. Analyze whole project and codes in detail and memory it. I am satisfied how it works, I just need some more updates. 
Tell me how to do that, but be precise where to add codes and when you tel me to change some code, please give me that whole part with update, so I can just copy the whole part and change the old part.

python manage.py runserver

.\.venv\Scripts\Activate.ps1

deactivate

python manage.py makemigrations
python manage.py migrate

.\export_structure.ps1 - kompletna struktura sa sadržajem

.\export_django_structure.ps1 - kompletna struktura sa sadržajem settings.py i Exercises i Blogs app-activate

Prevodjenje:

django-admin makemessages -l de
django-admin makemessages -l tr
python manage.py makemessages -l pl
python manage.py makemessages -l cs
django-admin compilemessages


Git commit and push commands(puštati jednu po jednu):

git add .
git commit -m "update"
git push

git push --force - nakon backup kopiranja


za makemigration:

if os.environ.get("DATABASE_URL"):
    DATABASES = {
        'default': dj_database_url.config(
            conn_max_age=600,
            ssl_require=True
        )
    }
else:
    DATABASES = {
        'default': {
            'ENGINE': 'django.db.backends.sqlite3',
            'NAME': os.path.join(BASE_DIR, 'db.sqlite3'),
        }
    }


